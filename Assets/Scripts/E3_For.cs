using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class E3_For : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int n;
        n = 4;
        for (int i = 1; i <= n; i++)
        {
            Debug.Log(i);
        }

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
